
services:
  client:
    build: 
      context: .
      dockerfile: Dockerfile
      args:
        DATABASE_URL: ${DATABASE_URL}
    container_name: comcamp37-service
    restart: unless-stopped
    env_file: .env
    ports:
      - 8980:3030
    networks:
      - internal

networks:
  internal: