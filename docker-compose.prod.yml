version: "3.9"

services:
  traefik:
    image: traefik:v3.4
    container_name: traefik
    restart: unless-stopped
    command:
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"

      - "--entrypoints.web.address=:80"

      - "--log.level=DEBUG"
      - "--accesslog=true"

      - "--api.dashboard=true"
      - "--api.insecure=true"

      - "--ping=true"
      - "--ping.entrypoint=health"
      - "--entrypoints.health.address=:8082"
    ports:
      - "8980:80"
      - "8080:8080"
      - "8082:8082"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - internal

  client:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file: .env
    labels:
      # Treafik Base Config
      - "traefik.enable=true"
      - "traefik.http.routers.client.rule=Host(`100.88.117.62`) || Host(`api.localhost`) || Host(`localhost:3030`) || Host(`dev-api2.comcamp.io`) || Host(`dev-api2.comcamp.io:4842`) || Host(`api.comcamp.io`) || Host(`dev-api.comcamp.io`) || Host(`localhost`)"

      - "traefik.http.routers.client.entrypoints=web"
      - "traefik.http.services.client.loadbalancer.server.port=3030"

      # Treafik Health Check Config
      - "traefik.http.services.client.loadbalancer.healthcheck.path=/health"
      - "traefik.http.services.client.loadbalancer.healthcheck.interval=5s"

      # - "traefik.http.middlewares.api-ratelimit.ratelimit.burst=2500"
      # - "traefik.http.routers.client.middlewares=api-ratelimit"
    networks:
      - internal

networks:
  internal:
    driver: bridge
